spring.application.name=hub
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/skillHub
spring.datasource.username=postgres
spring.datasource.password=Sasikala@2361

spring.jpa.hibernate.ddl-auto=update

# AI scoring (optional). Set these to enable OpenAI-based scoring.
# ai.scoring.enabled=true
# ai.scoring.apiKey=${OPENAI_API_KEY}
# ai.scoring.baseUrl=https://api.openai.com
# ai.scoring.model=gpt-4o-mini